# libX11 runtime cache directories pulled in by Prime's graphical runtime.
# bootc images keep /var mutable, so directory ownership is declared here
# rather than relying on package-install-time directory creation.
d /var/cache/libX11 0755 root root - -
d /var/cache/libX11/compose 0755 root root - -
