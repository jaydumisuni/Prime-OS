diff --git a/docs/contracts/PRIME_P1_SHELL_V1.md b/docs/contracts/PRIME_P1_SHELL_V1.md
new file mode 100644
--- /dev/null
+++ b/docs/contracts/PRIME_P1_SHELL_V1.md
@@ -0,0 +1,138 @@
+# Prime P1 Shell v1
+
+Status: **CONSTRUCTION DRAFT — NOT PRODUCT AUTHORITY**
+
+Depends on: `PRIME_P1_FRAME_LOOP_V1.md` acceptance and promotion.
+
+## Purpose
+
+Define the minimum Prime-owned user experience required for P1 First Light without expanding into P2 component delivery, Windows/Android personalities, Origins/Ptah integration, or a generic desktop-environment replacement.
+
+This contract refines the already-frozen Prime Shell requirements in `PRIME_OS_MASTER_PLAN.md`, `PRIME_OS_ROADMAP.md`, and `ADR-0001-P1-SYSTEM-FOUNDATION.md`. Those documents remain authoritative if this draft conflicts with them.
+
+## Ownership
+
+Prime Shell is a Prime-owned user-space client of the Prime compositor and Prime Core.
+
+P1 implementation structure:
+
+- `prime-compositor` remains Wayland/window/system-surface authority;
+- `prime-shell-host` is the lightweight Rust native Shell host;
+- `shell-ui/` contains React + TypeScript rich Shell surfaces;
+- rich surfaces use the system WebKit stack rather than Electron or a duplicated Chromium runtime;
+- `primed` remains machine/Core authority and is consumed through `/run/prime/core.sock`;
+- Shell does not move privileged display, workload, network, audio, power, update, or recovery authority out of their Prime owners.
+
+## Required P1 surfaces
+
+The first accepted Shell must provide unmistakable Prime identity and at minimum:
+
+1. Prime background/startup surface;
+2. Prime system rail/status surface;
+3. Prime Orb/launcher baseline;
+4. quick-controls surface with truthful network/audio/power/settings state;
+5. visible windowing path for ordinary XDG application surfaces;
+6. shutdown/restart entry points routed to Prime-owned authority;
+7. recovery entry that remains available independently if Shell fails.
+
+The visual gate remains owner-accepted and includes Prime glass/depth language, smooth core transitions, no obvious Fedora/GNOME/KDE/COSMIC identity, and reference-video comparison.
+
+## Wayland roles
+
+P1 Shell uses the compositor's existing Wayland protocols; it does not create a second compositor.
+
+Privileged Shell surfaces use WLR layer-shell namespaces owned by Prime. The initial namespace family is reserved as:
+
+- `prime.shell.background`
+- `prime.shell.rail`
+- `prime.shell.orb`
+- `prime.shell.quick-controls`
+
+Expected layer intent:
+
+- background: `Background`;
+- rail: `Top` with appropriate exclusive zone when it reserves workspace area;
+- Orb/launcher and quick controls: `Overlay` while open.
+
+Exact geometry and animation remain Shell policy, while mapping, stacking, input routing and output ownership remain compositor authority.
+
+## Readiness
+
+Process start is not Shell readiness.
+
+The P1 persistent baseline is `prime.shell.background` plus `prime.shell.rail`. Orb/launcher and quick controls remain required P1 capabilities, but they are transient overlays and are not required to remain mapped while closed. Closing those transient surfaces does not by itself invalidate `shell_ready`.
+
+`prime-compositor` may set `shell_ready=true` only after all of the following are true in one live compositor process:
+
+1. `frame_loop_ready=true` has already been earned or is earned by the same retiring selected-CRTC frame;
+2. the persistent background and rail are both mapped on their expected WLR layers;
+3. both persistent surfaces are owned by the same live Wayland client;
+4. both persistent surfaces contribute renderable Wayland surface elements when the frame is queued;
+5. the exact background and rail surface identities remain alive and mapped when that frame retires successfully on the matching selected-CRTC vblank;
+6. the Shell client therefore remains alive through that retirement.
+
+A startup process, socket connection, configure event, namespace string without a renderable buffer, mapped object without renderable content, generic non-Shell XDG client, or transient Orb/quick-controls surface must not set `shell_ready=true`.
+
+If either persistent surface exits, disappears, detaches its renderable buffer, Wayland protocol dispatch fails, output/frame authority is invalidated, or session graphics revalidation is required, `shell_ready` must fail closed until the persistent baseline is rendered and retired again.
+
+The `prime.shell.*` namespace family is a compositor role/readiness identity, not a standalone security or workload authorization mechanism. Prime workload/service authority remains separate.
+
+## Shell host / UI boundary
+
+The Rust host owns:
+
+- Wayland connection and lifecycle;
+- privileged layer-shell role creation;
+- system WebKit view hosting;
+- Core socket transport and version negotiation;
+- capability/readiness handoff to the UI;
+- input/activation plumbing;
+- fail-closed handling when required Prime authority is unavailable.
+
+React + TypeScript owns presentation and ordinary UI state for:
+
+- rail rendering;
+- Orb/launcher presentation;
+- quick-control presentation;
+- settings/navigation surfaces;
+- motion/glass/depth presentation.
+
+JavaScript must not directly acquire machine privileges. Mutations cross a typed Rust/Core boundary and are authorized by the owning Prime subsystem.
+
+## Core data truth
+
+P1 Shell displays only state that Prime currently owns and can report truthfully.
+
+The existing `prime.system.status` capability may provide read-only observed network link, audio-device, power and thermal truth. Where a mutation backend has not yet earned authority, the corresponding control is visibly unavailable rather than simulated.
+
+No UI state may imply NetworkManager/PipeWire/power mutation authority merely because the control is visually present.
+
+## Failure semantics
+
+Shell failure must not prevent:
+
+- Prime Core operation;
+- compositor failure reporting;
+- recovery entry;
+- system generation rollback/recovery;
+- machine shutdown through a recovery/console path.
+
+The Shell service may restart under systemd policy, but restart loops must not convert failure into readiness.
+
+## Explicitly unearned by this contract
+
+P1 Shell v1 does not claim:
+
+- Prime Store/component delivery;
+- Windows or Android personality UI;
+- Origins/Hunter/Ptah integration;
+- multi-output Shell policy;
+- full accessibility implementation;
+- touch/tablet/gesture completion;
+- presentation-time/VRR policy;
+- secure executable identity merely from a Wayland namespace;
+- owner visual acceptance before the owner performs it.
+
+## Construction acceptance sequence
+
+`FRAME accepted → Shell host/UI construction → static/build proof → compositor/Shell readiness integration proof → physical first-frame proof on the P1 host → owner visual gate → selective promotion`.
+diff --git a/docs/contracts/PRIME_COMPOSITOR_READINESS_V1.md b/docs/contracts/PRIME_COMPOSITOR_READINESS_V1.md
+--- a/docs/contracts/PRIME_COMPOSITOR_READINESS_V1.md
++++ b/docs/contracts/PRIME_COMPOSITOR_READINESS_V1.md
+@@ -11,7 +11,7 @@ Authority: `docs/contracts/PRIME_P1_SHELL_COMPOSITOR_V1.md`
+ 
+ Prime must distinguish compositor process existence from actual graphical responsibility. This contract defines the machine-readable readiness record for `prime-compositor` so Prime Core, proof tooling and later Prime Shell integration can reason about what the compositor has mechanically earned.
+ 
+-P1 defines four earned initialization phases:
++P1 defines successive earned initialization/runtime phases:
+ 
+ ```text
+ BACKEND_PREFLIGHT
+@@ -22,9 +22,15 @@ RENDERER_READY
+ OUTPUTS_READY
+       ↓
+ WAYLAND_PROTOCOLS_READY
++      ↓
++WAYLAND_INPUT_READY
++      ↓
++FRAME_LOOP_READY
++      ↓
++SHELL_READY
+ ```
+ 
+-`BACKEND_PREFLIGHT` and `RENDERER_READY` do not claim a configured display output. `OUTPUTS_READY` claims only the P1 minimum physical KMS responsibility: one selected connected output has a real Smithay DRM surface/compositor initialized through the retained single-GPU renderer path.
++`BACKEND_PREFLIGHT` and `RENDERER_READY` do not claim a configured display output. `OUTPUTS_READY` claims only the P1 minimum physical KMS responsibility: one selected connected output has a real Smithay DRM surface/compositor initialized through the retained single-GPU renderer path. Later runtime phases are independently earned and may fail closed without rewriting earlier protocol objects into stronger truth.
+@@ -49,7 +55,7 @@ The current P1 record contains:
+ {
+   "schema": "prime.compositor-readiness.v1",
+   "observed_at": "RFC3339",
+-  "phase": "BACKEND_PREFLIGHT|RENDERER_READY|OUTPUTS_READY|WAYLAND_PROTOCOLS_READY|WAYLAND_INPUT_READY|FRAME_LOOP_READY|FRAME_ERROR|FRAME_MAPPING_ERROR|WAYLAND_PROTOCOL_ERROR|OUTPUT_ERROR|OUTPUT_REVALIDATION_REQUIRED|SESSION_PAUSED|RENDERER_REVALIDATION_REQUIRED|SESSION_RESUME_FAILED",
++  "phase": "BACKEND_PREFLIGHT|RENDERER_READY|OUTPUTS_READY|WAYLAND_PROTOCOLS_READY|WAYLAND_INPUT_READY|FRAME_LOOP_READY|SHELL_READY|FRAME_ERROR|FRAME_MAPPING_ERROR|WAYLAND_PROTOCOL_ERROR|OUTPUT_ERROR|OUTPUT_REVALIDATION_REQUIRED|SESSION_PAUSED|RENDERER_REVALIDATION_REQUIRED|SESSION_RESUME_FAILED",
+@@ -72,6 +78,8 @@ The current P1 record contains:
+   "frames_queued": 0,
+   "frames_submitted": 0,
+   "mapped_surface_frames_submitted": 0,
++  "shell_frame_in_flight": false,
++  "shell_frames_submitted": 0,
+   "renderer_ready": true,
+   "outputs_ready": true,
+   "shell_ready": false,
+@@ -349,6 +357,18 @@ Current-process monotonic frame evidence counters. `frames_submitted <= frames_q
+ 
+ A successful libinput resume may restore this field while graphics remain in `RENDERER_REVALIDATION_REQUIRED`, because input delivery and renderer/output validation are separate authorities.
+ 
++### `shell_frame_in_flight`
++
++True only while the selected CRTC has one queued frame whose renderable layer-surface set contains the complete P1 persistent Shell baseline: `prime.shell.background` on `Background` plus `prime.shell.rail` on `Top`, both owned by the same live Wayland client. The exact two `wl_surface` identities are frozen with that in-flight frame and must still be alive and mapped at retirement.
++
++This flag does not claim that Orb/quick-controls are open and does not by itself earn `shell_ready`.
++
++### `shell_frames_submitted`
++
++Current-process monotonic count of selected-CRTC frames that retired after carrying the complete persistent Shell baseline and revalidating the exact background/rail surface identities at matching vblank.
++
++`shell_frames_submitted <= mapped_surface_frames_submitted <= frames_submitted`.
++
+ ### `renderer_ready`
+ 
+ May be `true` only in a phase that has earned the selected P1 single-GPU GBM/EGL/GLES renderer on the exact current session/device path.
+@@ -373,7 +393,13 @@ It is invalidated by session pause, a DRM notifier error, or a post-startup DRM
+ 
+ ### `shell_ready`
+ 
+-Owned by the Prime Shell integration gate. The compositor must never infer this merely from its own process, renderer, KMS output or protocol-global health.
++Owned by the Prime Shell integration gate. The compositor must never infer this merely from its own process, renderer, KMS output, accepted client count, namespace string or protocol-global health.
++
++It may become `true` only after `frame_loop_ready=true` has already been earned or is earned by the same retiring frame, the persistent background and rail are both mapped on their expected layers, both are renderable and owned by the same live Wayland client when the frame is queued, and the exact two surface identities remain alive/mapped when that frame retires through the matching selected-CRTC vblank.
++
++Loss/destruction of either persistent surface, a commit that makes the persistent baseline non-renderable, Wayland protocol failure, frame/output invalidation, DRM topology invalidation or session graphics invalidation clears `shell_ready` until the complete baseline earns another retirement.
++
++`SHELL_READY` does not imply Orb/quick-controls are currently open, system controls have earned mutation authority, secure executable identity has been proven from a namespace, or owner visual acceptance has occurred.
+ 
+ ### `clients_accepted`
